apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sql-server-stage
spec:
  serviceName: sql-server-stage
  replicas: 1
  selector:
    matchLabels:
      app: sql-server-stage
  template:
    metadata:
      labels:
        app: sql-server-stage
    spec:
      containers:
      - name: sql-server-stage
        image: gcr.io/constellation-275522/sql-server
        ports:
        - containerPort: 1433
          name: sql-server-stage
        env:
        - name: MSSQL_PID
          value: "Express"
        - name: ACCEPT_EULA
          value: "Y"
        - name: SA_PASSWORD
          valueFrom:
            secretKeyRef:
              name: sql-server-stage
              key: SA_PASSWORD
        resources:
          limits:
            cpu: "2"
            memory: "3000Mi"
          requests:
            cpu: "1"
            memory: "2000Mi"
        volumeMounts:
        - name: sql-server-stage
          mountPath: /var/opt/mssql
        livenessProbe:
          tcpSocket:
            port: 1433
          initialDelaySeconds: 3
          periodSeconds: 3
        securityContext:
          privileged: true
  volumeClaimTemplates:
  - metadata:
      name: sql-server-stage
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 10Gi
---          
apiVersion: v1
kind: Service
metadata:
  name: sql-server-stage
  labels:
    app: sql-server-stage
spec:
  ports:
  - port: 1433
    name: sql-server-stage
  clusterIP: None
  selector:
    app: sql-server-stage    