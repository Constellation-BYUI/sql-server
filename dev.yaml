apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sql-server-dev
spec:
  serviceName: sql-server-dev
  replicas: 1
  selector:
    matchLabels:
      app: sql-server-dev
  template:
    metadata:
      labels:
        app: sql-server-dev
    spec:
      containers:
      - name: sql-server-dev
        image: gcr.io/constellation-275522/sql-server
        ports:
        - containerPort: 1433
          name: sql-server-dev
        env:
        - name: MSSQL_PID
          value: "Express"
        - name: ACCEPT_EULA
          value: "Y"
        - name: SA_PASSWORD
          valueFrom:
            secretKeyRef:
              name: sql-server-dev
              key: SA_PASSWORD
        resources:
          limits:
            cpu: ".9"
            memory: "1000Mi"
          requests:
            cpu: ".9"
            memory: "1000Mi"
        volumeMounts:
        - name: sql-server-dev
          mountPath: /var/opt/mssql
        livenessProbe:
          tcpSocket:
            port: 1433
          initialDelaySeconds: 3
          periodSeconds: 3
        securityContext:
          privileged: true
  volumeClaimTemplates:
  - metadata:
      name: sql-server-dev
    spec:
      accessModes:
        - ReadWriteMany
      resources:
        requests:
          storage: 10Gi
---          
apiVersion: v1
kind: Service
metadata:
  name: sql-server-dev
  labels:
    app: sql-server-dev
spec:
  ports:
  - port: 1433
    name: sql-server-dev
  clusterIP: None
  selector:
    app: sql-server-dev    